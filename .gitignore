node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store

# server
server/prisma/dev.db
server/prisma/dev.db-journal
server/prisma/*.db
server/prisma/*.db-journal
server/generated/
server/uploads/
*.db
*.db-journal

# build caches / scratch
*.tsbuildinfo
vite.config.ts.timestamp-*
.temp/
.smoke-*.txt
.rw-*.json

# local-only smoke-test reports (see CLAUDE.md report convention) — never committed,
# except the round-7 report and its screenshots, which ship with the round (PO request).
/reports/*
!/reports/round7/
!/reports/round7-report.html

# agent/IDE local state
.claude/settings.local.json
.quadcodeai/

# macOS SwiftPM
macos/.build/
macos/.swiftpm/

# tracker local state (never commit a real device token)
.vibehub/
